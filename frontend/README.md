# Vue Frontend

The Vue application is implemented in Phase 6. It will consume versioned n8n webhook
contracts and render sanitized Markdown and private document assets. No RAG or ingestion
business logic belongs here.
